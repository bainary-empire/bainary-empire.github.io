# BAI Admin Portal v7

Admin-only portal for creating and managing BAI Personnel Portal accounts.

Demo administrator credentials:
- Email: admin@bai.local
- Password: BAI-admin

Personnel accounts are stored in the shared browser localStorage key `baiPersonnelAccounts`. For the prototype, serve the Admin Portal and Personnel Portal from the same origin so they can share this registry.
